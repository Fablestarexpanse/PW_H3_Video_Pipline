# worlds/fablestar

Canon shared across productions (Fablestar Expanse; Dead Signal if ruled an episode — MIGRATION 'decide'). Copied from the
Feyndral vault 2026-08-23, never linked; the vault is read-only. `refs/` holds the approved recurring turnarounds with
their manifest; `canon/` the Production Bible and Voice Locks verbatim as source material (to be split into
characters/*.md and locations/*.md in prompt words at that production's G2).

Not found in the vault: separate Perpetua / Greymoor canon folders named in MIGRATION.md — only `00 System`,
`01 Templates`, `Productions` exist. Create `worlds/perpetua`, `worlds/greymoor` when that material surfaces.
