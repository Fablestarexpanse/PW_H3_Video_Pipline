CANON (G2) for production $ARGUMENTS. One `characters/<key>.md` per recurring character and one `locations/<key>.md` per location, from `_TEMPLATE.md`: spec in prompt words, <= 2 adjectives per axis (LAWS 12), carried props in beats never here. Voice locks go in `identity.VOICES` as byte-identical (S1)/(S2) phrases.

World-owned canon (shared across productions) goes in `worlds/<world>/`, copied never linked. Run `python tools/contract.py <slug>`. G2 is every character file at `status: approved`. No pixel is rendered before G2.
